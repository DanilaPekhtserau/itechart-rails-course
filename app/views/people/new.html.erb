<h1>New person</h1>

<%= link_to 'Back', "/people" %>

<p>
<%= form_with model: @person do |f| %>
  <%= f.text_field :name, required: true, minlength: 2, placeholder: "Имя персоны" %>
  <%= f.submit "Submit person"%>
<% end %>